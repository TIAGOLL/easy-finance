{
  "version": 2,
  "builds": [
    {
      "src": "apps/api/src/main.ts",
      "use": "@vercel/node",
      "config": {
        "nodeVersion": "18.x",
        "packageManager": "pnpm",
        "installCommand": "pnpm install --filter api... --frozen-lockfile",
        "buildCommand": "pnpm db:migrate:deploy; pnpm generate; pnpm build --filter api...",
        "outputDirectory": "apps/api/dist"
      }
    }
  ],
  "routes": [
    {
      "src": "/(.*)",
      "dest": "apps/api/src/main.ts"
    }
  ]
}
